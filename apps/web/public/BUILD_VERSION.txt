glazetopia-checkers
phase: 5.0.11
signature: GLAZETOPIA_GAMECLIENT_SIGNATURE_v5_0_11

Key changes in 5.0.11:
  - interactive, isOver, and status-bar state now derive from view (server-authoritative)
    rather than phase. Even if phase lags, the board becomes interactive when
    view.status === 'active' and view.turn === 'player'.
  - narrowPhaseForStatusBar('pending-commit') → 'your-turn' (no more "Loading..." flash)
  - VISIBLE DEBUG PANEL: top-right corner of the page shows phase, view.status,
    view.turn, coverLifted, interactive, drawOffered live.

Look for the DEBUG panel in the top-right corner of the live page.
