glazetopia-checkers
phase: 5.0.10
signature: GLAZETOPIA_GAMECLIENT_SIGNATURE_v5_0_10

This file is served as a STATIC asset (no JS required to verify).
After deploy, fetch:  https://<vercel-domain>/BUILD_VERSION.txt

If this file returns 404 or shows an older phase, the deployed build does
NOT contain the 5.0.10 source code regardless of what GitHub shows.

Key changes in 5.0.10:
  - Auto-lift logic: const shouldLift = hashSkip || v.status !== 'pending';
    (replaces the older `inProgress = lastMove !== null || moveCount > 0`)
  - Signature constant: GLAZETOPIA_GAMECLIENT_SIGNATURE_v5_0_10
    survives minification - grep the live JS bundle for it
  - Console logs:
      [GameClient] LIVE GAMECLIENT LOADED - phase5.0.10
      [GameClient] SIGNATURE: GLAZETOPIA_GAMECLIENT_SIGNATURE_v5_0_10
